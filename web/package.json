{
  "name": "upstream-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync-feed": "node scripts/sync-feed.mjs",
    "predev": "npm run sync-feed",
    "dev": "next dev",
    "prebuild": "npm run sync-feed",
    "build": "next build",
    "precheck": "npm run sync-feed",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^16.0.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.0"
  },
  "overrides": {
    "postcss": "^8.5.18",
    "sharp": "^0.35.0"
  }
}
