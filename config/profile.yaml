name: Simar
domains:
  - aws-ecosystem
  - infrastructure-as-code
  - kubernetes-controllers
  - platform-tooling
  - gitops-delivery
  - observability
  - service-mesh
  - security-policy
  - identity-access
  - ci-cd-automation
preferred_shapes:
  - bug-fix
  - tests
  - chart-config
  - docs
skills:
  - id: terraform
    label: Terraform
    keywords: [terraform, terragrunt, opentofu, open tofu, hcl, provider, module]
  - id: helm
    label: Helm
    keywords: [helm, chart, values.yaml, template, release]
  - id: eks
    label: Amazon EKS
    keywords: [eks, amazon eks, aws eks]
  - id: iam-irsa
    label: AWS IAM and IRSA
    keywords: [iam, irsa, pod identity, workload identity, service account]
  - id: storage-csi
    label: Kubernetes storage and CSI
    keywords: [csi, storageclass, persistentvolume, ebs, efs, volume]
  - id: networking-ingress
    label: Kubernetes networking and ingress
    keywords: [ingress, gateway, load balancer, networking, cni, cilium, traefik, cert-manager, certificate, tls, dns, alb, nlb]
  - id: gitops
    label: GitOps
    keywords: [gitops, argocd, argo cd, flux, reconciliation]
  - id: go
    label: Go
    keywords: [golang, go code, go.mod]
  - id: aws-compute
    label: AWS compute and node lifecycle
    keywords: [ec2, autoscaling, auto scaling, launch template, instance type, node group, nodegroup]
  - id: kubernetes-platform
    label: Kubernetes and operators
    keywords: [kubernetes, k8s, operator, controller, custom resource, crd, admission webhook]
  - id: developer-workspaces
    label: Cloud developer workspaces
    keywords: [eclipse che, devworkspace, dev workspace, devfile, workspace operator]
  - id: identity-oidc
    label: Keycloak and OIDC
    keywords: [keycloak, oidc, openid connect, oauth, oauth2, single sign-on, sso, identity provider]
  - id: observability-stack
    label: Grafana and Prometheus observability
    keywords: [grafana, prometheus, alertmanager, kube-prometheus, metrics, telemetry, tracing, alerting]
  - id: cicd-pipelines
    label: CI/CD pipelines
    keywords: [azure pipelines, azure devops, gitlab ci, gitlab runner, jenkins, pipeline, build agent]
